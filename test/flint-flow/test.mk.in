.PHONY: all clean check

all: flow.txt

check: EXPECTED flow.txt
	diff -u $^

clean:
	-rm -f flow.txt output.db layout

output.db: INPUT
	test-input1 $@ $<
	flint-phml $@

layout: output.db
	flint-layout $< $@

flow.txt: output.db layout
	flint-flow $^ > $@
