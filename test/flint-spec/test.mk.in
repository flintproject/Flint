.PHONY: all clean check

all: spec.txt

check: EXPECTED spec.txt
	diff -u $^

clean:
	-rm -f output.db spec.txt

output.db: INPUT
	test-input1 $@ $<
	COMMAND $@

spec.txt: output.db
	flint-spec $< > $@
