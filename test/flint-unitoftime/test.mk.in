.PHONY: all clean check

all: output.db

clean:
	-rm -f *.db *.xml unitoftime

check: EXPECTED unitoftime
	cmp $^

output.db: INPUT
	test-input1 $@ $<
	flint-phml $@

unitoftime: output.db
	flint-unitoftime $< $@
