.PHONY: all clean check

all: OUTPUT_STDOUT

check: OUTPUT_STDOUT
	diff -u EXPECTED_STDOUT OUTPUT_STDOUT

clean:
	-rm -f OUTPUT_DB OUTPUT_STDOUT

OUTPUT_DB: INPUT
	test-input2 $@ $< N/A
	flint-sedml $@

OUTPUT_STDOUT: OUTPUT_DB
	flint-taskpref 1 $< > $@
